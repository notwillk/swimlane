# Simple board

A small set of tickets with no dependencies: mix of todo, in-progress, and done. Good for testing `swimlane ls`, default table output, and `swimlane next` (should return the p1 todo ticket).
