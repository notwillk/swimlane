# Tagged tickets

Tickets with varied tags (backend, frontend, auth, infra, ci, api, db). Use for testing filter flags: `swimlane ls --tag backend`, `swimlane ls --tag !infra`, `swimlane next --tag frontend`, etc.
