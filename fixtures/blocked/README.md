# Blocked dependencies

Tickets with `blocked_by` relationships. `add-auth` is the only unblocked todo; `implement-api` and `dashboard-ui` are blocked by it. Good for testing `swimlane next` (should return add-auth first; after marking it done, next would be one of the p1/p2 blocked tickets).
